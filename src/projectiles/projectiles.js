export class Projectiles {
    constructor(model) {
        // expose the model
        this.model = model;
    }
 
    update = function(interval) {
        // create new shots
        // move existing shots
            // resolve hits
    }
}
